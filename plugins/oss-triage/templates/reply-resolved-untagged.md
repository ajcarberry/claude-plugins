Thanks for the <detailed report | repro kit>. I confirmed <the symptom> is gone on the current main.

## Fix

Commit <sha><. The fix is @<author>'s PR #<origin>, which was folded into the amended PR #<vehicle> and landed with it>. <One or two sentences on what changed.> Not in a tagged release yet; it will ship with the next release after <vX.Y.Z>.

## Verified on main (<rev>, `<cli> --version output`)

- Platform: <OS, arch>
- Repository: <how it was set up>

```
<the reporter's commands>
```

Output:

```
<output>
```

<One sentence on what the output shows.>

Closing as fixed.
