Thanks for the write-up. <Product> actually does have this and I confirmed it on the current release.

But the only places it is described today are <where> (links below). Nothing in <the how-to guides, the CLI reference, or the quickstart> mentions it.

## Where it is documented

- <Feature>: [<doc section>](<url#anchor>). "<short quote>"
- <Adjacent feature worth knowing>: [<doc section>](<url#anchor>). <One sentence on how they combine.>

## Verified on <vX.Y.Z> (`<cli> --version output`)

- Platform: <OS, arch>
- Repository: <setup>

<Before and after commands and output, as blocks.>

Given the documentation gap, I have retitled this and relabelled it as documentation, and I am keeping it open to cover <what needs writing>.

<What they asked for that does not exist>: <say so plainly>. If the existing feature does not cover your case once you have tried it, let me know and we'll get an enhancement request raised to track that.
