Thanks for the repro kit. I ran it end to end and confirmed <the symptom> is gone on <vX.Y.Z>.

## Fix

Commit <sha>, shipped in <vX.Y.Z>. <One or two sentences on what changed.>

## Verified on <vX.Y.Z> (`<cli> --version output`)

- Platform: <OS, arch>
- Server: <server version and mode>

### What was run

<Describe the kit in words a stranger can follow: what repository or data, what the script does step by step, how many steps, what was adapted and why.>

### Result

Last step and totals:

```
<final output block>
```

<One sentence on what it shows.><If the run is a scaled proxy, say what it covers and what it does not.>

Closing as fixed.
