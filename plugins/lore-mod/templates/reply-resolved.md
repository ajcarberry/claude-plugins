Thanks for the <detailed report | repro kit | measurements>. I confirmed <the symptom> is gone on <vX.Y.Z>.

## Fix

Commit <sha><, which came in with PR #n>. <One or two sentences on what changed, in plain words.>

## Verified on <vX.Y.Z> (`<cli> --version output`)

- Platform: <OS, arch>
- Repository: <how it was set up, in one line>

```
<the reporter's commands, as run>
```

Output:

```
<the output that shows the fix>
```

<One sentence on what the output shows.>

Closing as fixed.
